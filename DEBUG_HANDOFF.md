# Phase-I Debug Handoff

تاریخ بسته: 2026-08-31

## هدف پروژه
این پروژه یک وب‌اپلیکیشن نقشه‌محور برای آماده‌سازی نقشه‌های Phase I، ترسیم و ویرایش مرز سایت، تصاویر هوایی تاریخی، زمین‌شناسی سطحی و سنگ‌بستر، و خروجی PDF/AutoCAD است.

## خواسته‌های محصول
- شماره پروژه پیش‌فرض: `AB-12345` (قابل ویرایش).
- دریافت یک‌باره‌ی Company Profile شامل نام، آدرس، تلفن، ایمیل، وب‌سایت و لوگو؛ استفاده‌ی خودکار در همه خروجی‌ها.
- امکان Apply Current Template to Project برای پروژه‌های قدیمی.
- نقشه‌های Street، Satellite و Toporama؛ داده‌های زمین‌شناسی از فایل‌های کاربر (MRD128/MRD126) و قابل جایگزینی.
- ترسیم مرز زمین/ساختمان با بستن خودکار به نقطه اول در انتهای ترسیم با Enter یا راست‌کلیک؛ امکان ویرایش بعدی.
- پیشنهاد مرز سایت از روی آدرس و اجازه‌ی ویرایش/تأیید توسط کاربر؛ در صورت نبود داده، آپلود تصویر و ترسیم دستی.
- تصاویر تاریخی: جست‌وجوی متادیتای منابع رسمی، انتخاب سال، پیش‌نمایش/کراپ، و آپلود دستی یا اسکرین‌شات مجاز از Google Earth؛ انتخاب نهایی با کاربر.
- خروجی combined PDF با انتخاب شیت‌های آماده و خروجی Editable AutoCAD ZIP همراه تصاویر و فایل‌های DXF.
- نمایش علت دقیق هر مانع خروجی به کاربر.
- مقیاس میله‌ای و وضعیت زوم ذخیره‌شده برای شیت‌ها.

## وضعیت فعلی و نکات عیب‌یابی
- این سند فقط وضعیت فنی و مراحل بازتولید را ثبت می‌کند؛ هیچ کلید، توکن، مسیر رایانه یا دادهٔ پروژهٔ واقعی نباید در آن قرار گیرد.
- برای بررسی، از commit/branch موردنظر خود استفاده کنید و وضعیت تغییرات را با `git status` بررسی کنید.
- خطای AutoCAD قبلی درباره‌ی `PlotStyleName` در جدول LAYER اصلاح شده (`390` اضافه شده)، اما باید در AutoCAD LT 2026 با یک فایل واقعی دوباره بازآزمایی شود.
- دانلود ZIP در محیط مرورگر تعبیه‌شده ممکن است رویداد دانلود را گزارش نکند؛ وضعیت موفقیت باید با وجود فایل روی دیسک بررسی شود.
- برای تصاویر Google، برنامه آن‌ها را خودکار از Google fetch/embed نمی‌کند؛ به‌دلیل محدودیت‌های مجوز و بازتولیدپذیری، کاربر باید نمای مجاز را ذخیره و از مسیر Manual upload وارد کند.
- لینک‌های دارای `?release=...` ممکن است نسخه‌ی قدیمی cache شده باشند؛ برای تست نسخه‌ی این بسته از branch/commit فعلی استفاده شود.

## اجرای محلی
نیازمندی: Node.js >= 24 و pnpm 11.

```powershell
pnpm install
pnpm test
pnpm build
```

تست‌هایی که با Node مستقیم اجرا شده‌اند:
- `tests/core.test.mjs`: 26 passed
- `tests/cad-ui.test.mjs`: 10 passed
- `tests/print-validation.test.mjs`: 9 passed
- `tests/cad-dxf.test.mjs`: 15 passed

همچنین `node --check app.js` موفق بوده است.

## ساختار مهم
- `app.js`, `index.html`, `styles.css`: رابط و جریان اصلی برنامه
- `src/`: منطق نقشه، خروجی، اعتبارسنجی چاپ، DXF و رابط CAD
- `data/`, `assets/`: داده‌ها و منابع نقشه/زمین‌شناسی
- `tests/`: تست‌های واحد و یکپارچه
- `scripts/`: اسکریپت build/deploy
- `docs/`: مستندات پروژه
- `.github/`: گردش‌کار GitHub Pages

## روش پیشنهادی بررسی
1. `pnpm install` و `pnpm test` را اجرا کنید.
2. با یک پروژه‌ی نمونه، Company Profile را ذخیره و Apply کنید.
3. آدرس را جست‌وجو کنید، مرز پیشنهادی را ویرایش و تأیید کنید.
4. برای هر Figure شیت A3 را ذخیره و مقیاس/زوم را بررسی کنید.
5. PDF و AutoCAD ZIP را بسازید.
6. تمام DXFهای ZIP را در AutoCAD LT 2026 باز کنید و خطاهای parser، واحدها، مختصات و تصاویر وابسته را بررسی کنید.

این فایل برای تحویل به عیب‌یاب نوشته شده است؛ فایل‌های نصب‌شده و خروجی تولیدی عمداً حذف شده‌اند تا حجم بسته کم و بازسازی قابل تکرار باشد. مسیرهای محلی و اطلاعات هویتی را به این مخزن اضافه نکنید.
